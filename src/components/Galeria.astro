---

---

<div id="galeria">
    <img
        src="/100/gale/1_collage.webp"
        alt="Skiing in mountains"
        class="galeria-img"
    />
    <img
        src="/100/gale/2_collage.webp"
        alt="Ski resort"
        class="galeria-img"
    />
    <img
        src="/100/gale/3_collage.webp"
        alt="Mountain skiing"
        class="galeria-img"
    />
    <img
        src="/100/gale/4_collage.webp"
        alt="Ski slopes"
        class="galeria-img"
    />
    <img
        src="/100/gale/5_collage.webp"
        alt="Winter sports"
        class="galeria-img"
    />
    <img
        src="/100/gale/6_collage.webp"
        alt="Ski school"
        class="galeria-img"
    />
    <img
        src="/100/gale/7_collage.webp"
        alt="Skiing in mountains"
        class="galeria-img"
    />
    <img
        src="/100/gale/8_collage.webp"
        alt="Ski resort"
        class="galeria-img"
    />
    <img
        src="/100/gale/9_collage.webp"
        alt="Mountain skiing"
        class="galeria-img"
    />
    <img
        src="/100/gale/10_collage.webp"
        alt="Ski slopes"
        class="galeria-img"
    />
    <img
        src="/100/gale/11_collage.webp"
        alt="Ski slopes"
        class="galeria-img"
    />
    <img
        src="/100/gale/12_collage.webp"
        alt="Ski slopes"
        class="galeria-img"
    />
    <img
        src="/100/gale/13_collage.webp"
        alt="Ski slopes"
        class="galeria-img"
    />
    <img
        src="/100/gale/14_collage.webp"
        alt="Ski slopes"
        class="galeria-img"
    />
</div>

<!-- Modal/Lightbox -->
<div id="lightbox" class="lightbox">
    <span class="close-btn">&times;</span>
    <img class="lightbox-content" id="lightbox-img" alt="" />
</div>

<style lang="scss">
    @use "/src/styles/variables.scss" as *;
    #galeria {
        column-count: 4;
        column-gap: 1rem;
        margin-bottom: $margen;
        @include hdplus {
            column-count: 5;
        }
        @include tableta-land {
            column-count: 3;
        }
        @include tableta {
            column-count: 3;
        }
        @include cel {
            padding: 3rem;
            column-count: 2;
        }
    }
    #galeria img {
        width: 100%;
        height: auto;
        margin-bottom: 1rem;
        cursor: pointer;
        transition:
            transform 0.3s ease,
            opacity 0.3s ease;
        
        /* Animación de entrada escalonada */
        animation: entradaGaleria;
        animation-timeline: view();
        animation-range: entry 0% cover 30%;
        animation-duration: 0.8s;
        animation-fill-mode: both;

        &:nth-child(1) { animation-delay: 0s; }
        &:nth-child(2) { animation-delay: 0.1s; }
        &:nth-child(3) { animation-delay: 0.2s; }
        &:nth-child(4) { animation-delay: 0.3s; }
        &:nth-child(5) { animation-delay: 0.4s; }
        &:nth-child(6) { animation-delay: 0.5s; }
        &:nth-child(7) { animation-delay: 0.6s; }
        &:nth-child(8) { animation-delay: 0.7s; }
        &:nth-child(9) { animation-delay: 0.8s; }
        &:nth-child(10) { animation-delay: 0.9s; }
        &:nth-child(11) { animation-delay: 1s; }
        &:nth-child(12) { animation-delay: 1.1s; }
        &:nth-child(13) { animation-delay: 1.2s; }
        &:nth-child(14) { animation-delay: 1.3s; }

        &:hover {
            transform: scale(1.02);
            opacity: 0.9;
        }
    }

    /* Lightbox styles */
    .lightbox {
        display: none;
        position: fixed;
        z-index: 9999;
        left: 0;
        top: 0;
        width: 100%;
        height: 100%;
        background-color: rgba(0, 0, 0, 0.9);
        backdrop-filter: blur(5px);
        animation: fadeIn 0.3s ease;
    }

    .lightbox.active {
        display: flex;
        justify-content: center;
        align-items: center;
    }

    .lightbox-content {
        max-width: 90%;
        max-height: 90%;
        object-fit: contain;
        animation: zoomIn 0.3s ease;
        box-shadow: 0 0 50px rgba(0, 0, 0, 0.5);
    }

    .close-btn {
        position: absolute;
        top: 20px;
        right: 40px;
        color: #fff;
        font-size: 50px;
        font-weight: bold;
        cursor: pointer;
        transition: all 0.3s ease;
        z-index: 10000;

        &:hover {
            color: #ff4444;
            transform: scale(1.2);
        }
    }

    @keyframes fadeIn {
        from {
            opacity: 0;
        }
        to {
            opacity: 1;
        }
    }

    @keyframes entradaGaleria {
        from {
            opacity: 0;
            transform: translateY(30px);
            filter: blur(10px);
        }
        to {
            opacity: 1;
            transform: translateY(0);
            filter: blur(0px);
        }
    }

    @keyframes zoomIn {
        from {
            transform: scale(0.8);
            opacity: 0;
        }
        to {
            transform: scale(1);
            opacity: 1;
        }
    }
</style>

<script>
    // Obtener elementos
    const lightbox = document.getElementById("lightbox");
    const lightboxImg = document.getElementById(
        "lightbox-img",
    ) as HTMLImageElement;
    const closeBtn = document.querySelector(".close-btn");
    const galeriaImages = document.querySelectorAll(".galeria-img");

    // Agregar evento click a cada imagen de la galería
    galeriaImages.forEach((img) => {
        img.addEventListener("click", () => {
            const imgElement = img as HTMLImageElement;
            if (lightbox && lightboxImg) {
                lightbox.classList.add("active");
                lightboxImg.src = imgElement.src.replace("w=600", "w=1200"); // Cargar versión más grande
                lightboxImg.alt = imgElement.alt;
                document.body.style.overflow = "hidden"; // Prevenir scroll
            }
        });
    });

    // Cerrar al hacer clic en el botón X
    closeBtn?.addEventListener("click", () => {
        closeLightbox();
    });

    // Cerrar al hacer clic fuera de la imagen
    lightbox?.addEventListener("click", (e) => {
        if (e.target === lightbox) {
            closeLightbox();
        }
    });

    // Cerrar con la tecla ESC
    document.addEventListener("keydown", (e) => {
        if (e.key === "Escape" && lightbox?.classList.contains("active")) {
            closeLightbox();
        }
    });

    function closeLightbox() {
        if (lightbox) {
            lightbox.classList.remove("active");
            document.body.style.overflow = ""; // Restaurar scroll
        }
    }
</script>

---
import Button from "./Button.astro";
interface Props {
 recuadro: string;
 parrafo: string;   
}
const { recuadro, parrafo } = Astro.props;
const baseUrl = import.meta.env.BASE_URL;
---

<div id="magazine" style={`--base-url: ${baseUrl}`}>
    <div class="izq animaScroll">
        <img
            src="/100/magazine.webp"
            alt="Ess Ski School 100 ans magazine pdf telechargue"
        />
    </div>
    <div class="der animaScroll2">
        <div class="centro ">
            <div class="recuadro-rojo">{recuadro}</div>
            <div class="conte-centro">
                <p set:html={parrafo}></p>
                <Button
                    url={`${baseUrl}magazine.pdf`}
                    text="Magazine PDF"
                    icon="mdi:tray-download"
                    target="_blank"
                />
            </div>
        </div>
    </div>
</div>

<style lang="scss">
    @use "../styles/variables.scss" as *;

     .animaScroll {
    animation: entradaScroll;
    animation-timeline: view();
    animation-range: 0% cover 100vh;
    
    @include cel {
      animation: entradaScrollMobile;
      animation-timeline: view();
      animation-range: 0% cover 70vh;
    }
  }

  .animaScroll2 {
    animation: entradaScroll2;
    animation-timeline: view();
    animation-range: 0% cover 110vh;
    
    @include cel {
      animation: entradaScroll2Mobile;
      animation-timeline: view();
      animation-range: 0% cover 60vh;
    }
  }

  @keyframes entradaScroll {
    from {
      opacity: 0;
      transform: translateX(130px);
    }
    to {
      opacity: 1;
      transform: translateX(0px);
    }
  }

  @keyframes entradaScrollMobile {
    from {
      opacity: 0;
      transform: translateY(130px);
    }
    to {
      opacity: 1;
      transform: translateY(0px);
    }
  }

  @keyframes entradaScroll2 {
    from {
      opacity: 0;
      transform: translateX(100px);
    }
    to {
      opacity: 1;
      transform: translateX(0px);
    }
  }

  @keyframes entradaScroll2Mobile {
    from {
      opacity: 0;
      transform: translateY(130px);
    }
    to {
      opacity: 1;
      transform: translateY(0px);
    }
  }


    #magazine {
        display: grid;
        grid-template-columns: 1fr 1fr;
        margin: 0 0 $margen 0;
    }
    .izq {
        grid-column: 1 / span 1;

        img {
            width: 100%;
            height: auto;
            object-fit: cover;
        }
    }
    .der {
        background: url("/100/magazine-bg.webp") no-repeat center center/cover;
        grid-column: 2 / span 1;
        place-content: center;

        .centro {
            min-height: 80%;
            max-width: 90%;
            padding: $margen-cel;
            transform: translateX(-10%);
            background: rgba($fondo, 0.6);
            backdrop-filter: blur(5px);
            @extend .sombra;
            display: flex;
            justify-content: center;
            align-items: center;
            .conte-centro{
                flex: 4;
                
            }
            
            .recuadro-rojo {
                flex: 1;
                transform: translateX(-30%);
                text-align: center;
            }
        }
    }
    
    @include cel{
        #magazine{
            grid-template-columns: 1fr;
            margin: 0 0;
        }
        .izq{
            img{
                aspect-ratio: 1.3/1;
            }   
        }
        .der{
            grid-column: 1 / span 1;
            display: flex;
            justify-content: center;
            .centro{
                transform: translate(0%, -10%);
                flex-direction:column;
                text-align: center;
                gap: 2rem;
                .conte-centro{
                    p{
                     margin-bottom: 2rem;   
                    }
                }
                .recuadro-rojo{
                    transform: translateX(0%);
                }
            }
        }
    }
</style>
